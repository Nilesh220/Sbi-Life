export const metadata = {
  title: 'The Competition',
  description: "Everything about IdeationX 2026 — competition phases, eligibility criteria, prize breakdown, jury panel, and key dates. Your complete guide.",
};

export default function Layout({ children }) {
  return children;
}

