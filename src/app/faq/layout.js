export const metadata = {
  title: 'FAQ',
  description: 'Answers to common IdeationX 2026 questions about eligibility, submission format, evaluation criteria, prizes, and support.',
};

export default function Layout({ children }) {
  return children;
}
